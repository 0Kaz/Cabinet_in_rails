!!!
%html
%head
  %title WiKinimous | Write what's on your mind anonymously !
  = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true
  = javascript_include_tag 'application', 'data-turbolinks-track' => true
  = csrf_meta_tags
%body
  %header
    .header_inner
      = link_to "Wikinimous", root_path, id: "logo"
      %nav
        - if user_signed_in?
          = link_to "New Thought", new_doc_path
          = link_to "Log out", destroy_user_session_path, method: :delete

        - else
          = link_to "Log In", new_user_session_path
  %p.notice= notice
  %p.alert= alert

  = yield
